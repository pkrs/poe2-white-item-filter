{
  "name": "poe2-equipment-planner",
  "private": true,
  "workspaces": [
    "packages/types",
    "packages/crawler",
    "packages/web"
  ],
  "scripts": {
    "build": "npm run build --workspace=@poe2-tfilter/types && npm run build --workspace=@poe2-tfilter/crawler && npm run build --workspace=@poe2-tfilter/web",
    "dev": "npm run dev --workspace=@poe2-tfilter/web",
    "crawl": "npm run crawl --workspace=@poe2-tfilter/crawler"
  }
}
